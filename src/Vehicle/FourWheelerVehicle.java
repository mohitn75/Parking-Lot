package Vehicle;

public class FourWheelerVehicle extends Vehicle implements VehicleInterface{
    public FourWheelerVehicle(String vehicleNumber, VehicleType vehicleType) {
        super(vehicleNumber, vehicleType);
    }
}
